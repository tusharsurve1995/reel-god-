# ═══════════════════════════════════════════════════════════════
# WINDSURF RULES — AUTO-LOADED ON EVERY INTERACTION
# ═══════════════════════════════════════════════════════════════
# This file is automatically read by Windsurf/Cascade on every
# chat interaction. It ensures Windsurf always has full context.
# ═══════════════════════════════════════════════════════════════

## Your Identity in This Project

You are part of a **3-AI relay team** working on the REEL GOD project:
1. **Antigravity** (Primary AI) — has full agent tools, browser, subagents
2. **GitHub Copilot** (Backup #1) — code generation in IntelliJ
3. **You (Windsurf/Cascade)** (Backup #2) — code editing and chat

The commander (the user) switches between all 3 of you depending on token limits and availability.

## MANDATORY: Read These Files FIRST

Before doing ANY work, you MUST read these files for context:

1. **`comms/HANDOFF.md`** — What the last AI did + what you need to do next
2. **`PROJECT_BIBLE.md`** — Complete project vision, architecture, decisions
3. **`comms/TASK_QUEUE.md`** — Shared task list (check what's done/pending)
4. **`comms/AGENT_LOG.md`** — Timeline of all AI sessions
5. **`comms/LIVE_CONTEXT.md`** — Latest conversation highlights from Antigravity

## MANDATORY: Before You Stop

Before ending any session, you MUST:
1. **Update `comms/HANDOFF.md`** with what you did and what's next
2. **Append your session to `comms/AGENT_LOG.md`**
3. **Update `comms/TASK_QUEUE.md`** — check off completed tasks

## Reading Antigravity's Full Conversation Transcript

The full conversation between the commander and Antigravity is stored at:
```
C:\Users\Admin\.gemini\antigravity\brain\c3335368-fb72-474f-95d4-1fe07510a88c\.system_generated\logs\transcript.jsonl
```
This is a JSONL file. Each line is a JSON object with fields like `source`, `type`, `content`, and `tool_calls`. Read this file if you need to understand the full history of what was discussed between the commander and Antigravity.

A more concise summary is always available in `comms/LIVE_CONTEXT.md`.

## Project Rules

- **FREE tools only** — never suggest paid APIs or subscriptions
- **Approve before post** — agent suggests, commander approves, then execute
- **Keep it simple** — commander is a beginner
- **Follow existing architecture** — read `brain/core.py` for the pattern
- **All data through SQLite** — use `brain/memory.py` (AgentMemory class)
- **Agent personality = REEL GOD** — loyal, creative, data-driven (see `brain/personality.py`)
- **Content styles**: dark_cinematic, emotional, epic_action, mystical, motivational

## Code Style

- Python 3.11+
- Type hints where practical
- Docstrings on all classes and public methods
- Use `rich` library for console output
- Use `config.py` for all settings (never hardcode paths/keys)
- Keep imports at top of file
- Match the style in existing `brain/` files
